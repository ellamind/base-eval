# Winogrande German BPB (answer-only, OLMES-style)
# BPB = -log2 P(continuation | prefix + correct_option) / bytes(continuation)

task: deu_winogrande_bpb
dataset_path: ellamind/winogrande-multilingual
dataset_name: deu
validation_split: validation
fewshot_config:
  sampler: first_n
  samples: !function utils.list_fewshot_samples
output_type: loglikelihood
target_delimiter: ""
doc_to_text: !function utils.doc_to_text_bpb
doc_to_target: !function utils.doc_to_target
num_fewshot: 5
process_results: !function utils.process_results_bpb
metric_list:
  - metric: bits_per_byte
    aggregation: mean
    higher_is_better: false
metadata:
  version: 1.0
  description: "Winogrande German - BPB evaluation"
